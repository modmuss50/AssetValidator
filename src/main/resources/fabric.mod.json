{
  "schemaVersion": 1,
  "id": "assetvalidator",
  "version": "${version}",
  "name": "Asset Validator",
  "description": "Asset Validator a mod that validates the assets",
  "environment": "client",
  "depends": {
    "fabricloader": ">=0.7.4"
  },
  "mixins" : [
    "assetvalidator.mixins.json"
  ]
}
